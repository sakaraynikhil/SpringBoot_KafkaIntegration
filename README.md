# SpringBoot_KafkaIntegration
This repository contains the working of Springboot with Apache kafka(Microservices communication asynchronous).


src-> Folder contains the sender microservice which sends the messages to the kafka topic

src-> Folder contains the receiver microservice which consumes the messages from the kafka topic.
